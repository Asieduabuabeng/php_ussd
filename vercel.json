{
    "version": 2,
    "builds": [
        {
            "src": "api/ussd.php",
            "use": "@vercel/php"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/ussd.php"
        }
    ]
}